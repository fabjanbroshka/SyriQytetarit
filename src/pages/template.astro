---
import '../styles/global.css';
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content="Citizen's Eye — Report city problems in 30 seconds." />
    <title>Citizen's Eye</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet" />
  </head>

  <body>

    <!-- ═══ NAV ═══ -->
    <header class="nav" id="mainNav">
      <div class="nav-inner">
        <a href="#" class="nav-logo">
          <svg width="20" height="20" viewBox="0 0 48 48" fill="none">
            <circle cx="24" cy="24" r="21" stroke="#2563eb" stroke-width="2.5"/>
            <ellipse cx="24" cy="24" rx="9" ry="6.5" stroke="#0f172a" stroke-width="2"/>
            <circle cx="24" cy="24" r="3" fill="#2563eb"/>
            <circle cx="24" cy="24" r="1.2" fill="white"/>
          </svg>
          Citizen's Eye
        </a>
        <nav class="nav-links" id="navLinks">
          <a href="#what">What</a>
          <a href="#why">Why</a>
          <a href="#how">How</a>
          <a href="#who">For who</a>
        </nav>
        <div class="nav-actions">
          <a href="#cta" class="btn-primary-sm">Get notified</a>
          <button class="burger" id="burger" aria-label="Menu">
            <span></span><span></span><span></span>
          </button>
        </div>
      </div>
      <!-- Mobile drawer -->
      <div class="nav-drawer" id="navDrawer">
        <a href="#what">What is it?</a>
        <a href="#why">Why?</a>
        <a href="#how">How it works?</a>
        <a href="#who">For who?</a>
        <a href="#cta" class="drawer-cta">Get notified</a>
      </div>
    </header>

    <!-- ═══ HERO ═══ -->
    <section class="hero" id="top">
      <div class="hero-inner">
        <div class="hero-badge fade-up" style="--d:0s">
          <span class="badge-ping"></span> App in development
        </div>

        <h1 class="hero-h1 fade-up" style="--d:0.1s">
          <em>Watch.</em><br/>
          Report.<br/>
          <span class="h1-change">Change.</span>
        </h1>

        <div class="hero-right fade-up" style="--d:0.25s">
          <p class="hero-desc">An app for reporting city problems — improper parking, damaged roads, pollution, and more — in under 30 seconds.</p>
          <div class="hero-stores">
            <a href="#cta" class="store-pill">
              <svg width="18" height="18" viewBox="0 0 814 1000" fill="currentColor"><path d="M788.1 340.9c-5.8 4.5-108.2 62.2-108.2 190.5 0 148.4 130.3 200.9 134.2 202.2-.6 3.2-20.7 71.9-68.7 141.9-42.8 61.6-87.5 123.1-155.5 123.1s-85.5-39.5-164-39.5c-76 0-103.7 40.8-165.9 40.8s-105-57.8-155.5-127.4C46 376.8 0 246.4 0 121.2 0 56.9 20.2 14.2 60.3 14.2c35.3 0 68.7 21.1 95.7 21.1 37.1 0 83.3-22.4 127.3-22.4 45.1 0 104.1 16.1 140.4 68.5h1.9c-4.5-21.6-10.9-50.3-10.9-74.6 0-58.4 33.5-100.7 76-117.5 5.8 15.3 9.6 33.5 9.6 52.4 0 64.9-46.5 117.5-84.8 117.5h-1.9c3.8 23 47.2 58.4 93.5 58.4 35.3 0 65.5-18.2 91.3-44.5l15.4 8.7z"/></svg>
              App Store
            </a>
            <a href="#cta" class="store-pill">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M3.18 23.76c.32.18.68.24 1.04.17l11.3-11.3-2.98-2.98L3.18 23.76z"/><path d="M22.38 10.54l-3.03-1.72-3.35 3.35 3.35 3.35 3.04-1.73c.87-.49.87-1.76-.01-2.25z"/><path d="M1.26 1.1C1.1 1.36 1 1.67 1 2.02v19.96c0 .35.1.66.26.92l.1.1L13.54 10.8v-.29L1.36 1z"/><path d="M13.54 14.21l-2.17-2.17L1.26 22.97c.37.39.95.43 1.38.1l10.9-8.86z"/></svg>
              Google Play
            </a>
          </div>
        </div>
      </div>

      <div class="hero-ticker">
        <div class="ticker-track">
          <span>Improper Parking</span><span class="ticker-dot">·</span>
          <span>Damaged Roads</span><span class="ticker-dot">·</span>
          <span>Pollution</span><span class="ticker-dot">·</span>
          <span>Broken Lighting</span><span class="ticker-dot">·</span>
          <span>Sidewalk Blockage</span><span class="ticker-dot">·</span>
          <span>Dangerous Behaviour</span><span class="ticker-dot">·</span>
          <span>Improper Parking</span><span class="ticker-dot">·</span>
          <span>Damaged Roads</span><span class="ticker-dot">·</span>
          <span>Pollution</span><span class="ticker-dot">·</span>
          <span>Broken Lighting</span><span class="ticker-dot">·</span>
          <span>Sidewalk Blockage</span><span class="ticker-dot">·</span>
          <span>Dangerous Behaviour</span><span class="ticker-dot">·</span>
        </div>
      </div>
    </section>

    <!-- ═══ WHAT IS IT ═══ -->
    <section class="section bg-white" id="what">
      <div class="container">
        <div class="section-top reveal">
          <span class="eyebrow">What is it?</span>
          <h2 class="section-h2">A civic tool built<br class="br-desk"/> for your city.</h2>
        </div>
        <div class="what-grid">
          <div class="what-card reveal" style="--ci:0">
            <div class="wc-icon" style="--cc:#ef4444">
              <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="1" y="3" width="15" height="13" rx="2"/><path d="M16 8h4l3 3v5h-7V8z"/><circle cx="5.5" cy="18.5" r="2.5"/><circle cx="18.5" cy="18.5" r="2.5"/></svg>
            </div>
            <h3>Improper Parking</h3>
            <p>Vehicles on sidewalks, crossings, bus stops, or disabled spaces.</p>
          </div>
          <div class="what-card reveal" style="--ci:1">
            <div class="wc-icon" style="--cc:#f59e0b">
              <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 17l4-8 4 4 3-6 4 10"/><path d="M3 21h18"/></svg>
            </div>
            <h3>Damaged Roads</h3>
            <p>Potholes, broken pavement, cracked curbs, missing covers.</p>
          </div>
          <div class="what-card reveal" style="--ci:2">
            <div class="wc-icon" style="--cc:#10b981">
              <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 2a10 10 0 1010 10"/><path d="M8 14s1.5 2 4 2 4-2 4-2"/><line x1="9" y1="9" x2="9.01" y2="9"/><line x1="15" y1="9" x2="15.01" y2="9"/></svg>
            </div>
            <h3>Pollution</h3>
            <p>Illegal dumping, overflowing bins, environmental hazards.</p>
          </div>
          <div class="what-card reveal" style="--ci:3">
            <div class="wc-icon" style="--cc:#8b5cf6">
              <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="5"/><line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/><line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/><line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/><line x1="1" y1="12" x2="3" y2="12"/><line x1="21" y1="12" x2="23" y2="12"/></svg>
            </div>
            <h3>Broken Lighting</h3>
            <p>Dark streets, faulty streetlights, unsafe public spaces at night.</p>
          </div>
          <div class="what-card reveal" style="--ci:4">
            <div class="wc-icon" style="--cc:#3b82f6">
              <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M17 21v-2a4 4 0 00-4-4H5a4 4 0 00-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 00-3-3.87"/></svg>
            </div>
            <h3>Sidewalk Blockage</h3>
            <p>Obstructions stopping pedestrians, cyclists, or wheelchair users.</p>
          </div>
          <div class="what-card what-card-cta reveal" style="--ci:5">
            <p class="wc-cta-text">And any other urban issue affecting your city.</p>
            <a href="#cta" class="btn-outline-sm">Join waitlist →</a>
          </div>
        </div>
      </div>
    </section>

    <!-- ═══ WHY ═══ -->
    <section class="section bg-ink" id="why">
      <div class="container">
        <div class="why-layout">
          <div class="why-left reveal">
            <span class="eyebrow eyebrow-light">Why?</span>
            <h2 class="section-h2 white">Problems go unsolved<br class="br-desk"/> because they're<br class="br-desk"/> invisible.</h2>
          </div>
          <div class="why-right reveal">
            <p class="why-body">When a pothole appears, most people notice — almost no one reports it. No data means no priority, no priority means no fix. The problem just stays.</p>
            <p class="why-body">Citizen's Eye makes it simple. A 30-second report from your pocket creates a structured data point that reaches the people who can actually do something about it.</p>
            <div class="why-numbers">
              <div class="why-num">
                <span class="wn-val">30s</span>
                <span class="wn-label">to file a report</span>
              </div>
              <div class="why-num">
                <span class="wn-val">0</span>
                <span class="wn-label">bureaucracy</span>
              </div>
              <div class="why-num">
                <span class="wn-val">100%</span>
                <span class="wn-label">citizen-driven</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- ═══ HOW IT WORKS ═══ -->
    <section class="section bg-white" id="how">
      <div class="container">
        <div class="section-top reveal">
          <span class="eyebrow">How it works?</span>
          <h2 class="section-h2">Five steps.<br class="br-mob"/> Thirty seconds.</h2>
        </div>
        <div class="steps">
          <div class="step reveal" style="--si:0">
            <div class="step-head">
              <span class="step-n">01</span>
              <div class="step-icon si-blue">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>
              </div>
            </div>
            <h3>You see a problem</h3>
            <p>Notice anything wrong in your street, neighbourhood, or public space.</p>
          </div>
          <div class="step-connector reveal" style="--si:0"></div>
          <div class="step reveal" style="--si:1">
            <div class="step-head">
              <span class="step-n">02</span>
              <div class="step-icon si-amber">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2"><path d="M23 19a2 2 0 01-2 2H3a2 2 0 01-2-2V8a2 2 0 012-2h4l2-3h6l2 3h4a2 2 0 012 2z"/><circle cx="12" cy="13" r="4"/></svg>
              </div>
            </div>
            <h3>Take a photo or video</h3>
            <p>Document the issue from your phone camera directly inside the app.</p>
          </div>
          <div class="step-connector reveal" style="--si:1"></div>
          <div class="step reveal" style="--si:2">
            <div class="step-head">
              <span class="step-n">03</span>
              <div class="step-icon si-green">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2"><path d="M9 11l3 3L22 4"/><path d="M21 12v7a2 2 0 01-2 2H5a2 2 0 01-2-2V5a2 2 0 012-2h11"/></svg>
              </div>
            </div>
            <h3>Choose the category</h3>
            <p>Pick from a clear list so your report is properly classified and routed.</p>
          </div>
          <div class="step-connector reveal" style="--si:2"></div>
          <div class="step reveal" style="--si:3">
            <div class="step-head">
              <span class="step-n">04</span>
              <div class="step-icon si-purple">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0118 0z"/><circle cx="12" cy="10" r="3"/></svg>
              </div>
            </div>
            <h3>Location set automatically</h3>
            <p>GPS pinpoints your exact spot. Adjust on the map if you need more precision.</p>
          </div>
          <div class="step-connector reveal" style="--si:3"></div>
          <div class="step reveal" style="--si:4">
            <div class="step-head">
              <span class="step-n">05</span>
              <div class="step-icon si-blue">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2"><line x1="22" y1="2" x2="11" y2="13"/><polygon points="22 2 15 22 11 13 2 9 22 2"/></svg>
              </div>
            </div>
            <h3>Done — report sent</h3>
            <p>Your report is submitted, verified, and tracked. You'll get updates as it progresses.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- ═══ FOR WHO ═══ -->
    <section class="section bg-soft" id="who">
      <div class="container">
        <div class="who-layout">
          <div class="who-left reveal">
            <span class="eyebrow">For who?</span>
            <h2 class="section-h2">For every citizen<br class="br-desk"/> who wants their<br class="br-desk"/> city to improve.</h2>
            <p class="who-body">No waiting. No bureaucracy. No need to know who to call. Just open the app, report what you see, and let the data do the work.</p>
            <div class="who-tags">
              <span>Commuters</span>
              <span>Residents</span>
              <span>Parents</span>
              <span>Cyclists</span>
              <span>Shop owners</span>
              <span>Students</span>
              <span>Anyone</span>
            </div>
          </div>
          <div class="who-right reveal">
            <blockquote class="pullquote">
              "No waiting.<br/>No bureaucracy.<br/>Just report."
            </blockquote>
          </div>
        </div>
      </div>
    </section>

    <!-- ═══ CTA ═══ -->
    <section class="section bg-ink cta-section" id="cta">
      <div class="container">
        <div class="cta-inner reveal">
          <span class="eyebrow eyebrow-light">Be the first</span>
          <h2 class="section-h2 white">Citizen's Eye<br/>is coming.</h2>
          <p class="cta-sub">Leave your email and we'll notify you the moment it launches.</p>
          <form class="cta-form" id="mainForm">
            <div class="cta-input-wrap">
              <input type="email" placeholder="your@email.com" required class="cta-input" />
              <button type="submit" class="cta-submit">Notify me</button>
            </div>
            <p class="cta-note">No spam. Unsubscribe anytime.</p>
          </form>
          <div class="cta-stores">
            <a href="#" class="cta-store-btn">
              <svg width="20" height="20" viewBox="0 0 814 1000" fill="currentColor"><path d="M788.1 340.9c-5.8 4.5-108.2 62.2-108.2 190.5 0 148.4 130.3 200.9 134.2 202.2-.6 3.2-20.7 71.9-68.7 141.9-42.8 61.6-87.5 123.1-155.5 123.1s-85.5-39.5-164-39.5c-76 0-103.7 40.8-165.9 40.8s-105-57.8-155.5-127.4C46 376.8 0 246.4 0 121.2 0 56.9 20.2 14.2 60.3 14.2c35.3 0 68.7 21.1 95.7 21.1 37.1 0 83.3-22.4 127.3-22.4 45.1 0 104.1 16.1 140.4 68.5h1.9c-4.5-21.6-10.9-50.3-10.9-74.6 0-58.4 33.5-100.7 76-117.5 5.8 15.3 9.6 33.5 9.6 52.4 0 64.9-46.5 117.5-84.8 117.5h-1.9c3.8 23 47.2 58.4 93.5 58.4 35.3 0 65.5-18.2 91.3-44.5l15.4 8.7z"/></svg>
              <div><span>Download on</span><strong>App Store</strong></div>
            </a>
            <a href="#" class="cta-store-btn">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M3.18 23.76c.32.18.68.24 1.04.17l11.3-11.3-2.98-2.98L3.18 23.76z"/><path d="M22.38 10.54l-3.03-1.72-3.35 3.35 3.35 3.35 3.04-1.73c.87-.49.87-1.76-.01-2.25z"/><path d="M1.26 1.1C1.1 1.36 1 1.67 1 2.02v19.96c0 .35.1.66.26.92l.1.1L13.54 10.8v-.29L1.36 1z"/><path d="M13.54 14.21l-2.17-2.17L1.26 22.97c.37.39.95.43 1.38.1l10.9-8.86z"/></svg>
              <div><span>Get it on</span><strong>Google Play</strong></div>
            </a>
          </div>
        </div>
      </div>
    </section>

    <!-- ═══ FOOTER ═══ -->
    <footer class="footer">
      <div class="container">
        <div class="footer-top">
          <a href="#top" class="nav-logo footer-logo">
            <svg width="18" height="18" viewBox="0 0 48 48" fill="none">
              <circle cx="24" cy="24" r="21" stroke="#2563eb" stroke-width="2.5"/>
              <ellipse cx="24" cy="24" rx="9" ry="6.5" stroke="#0f172a" stroke-width="2"/>
              <circle cx="24" cy="24" r="3" fill="#2563eb"/>
              <circle cx="24" cy="24" r="1.2" fill="white"/>
            </svg>
            Citizen's Eye
          </a>
          <nav class="footer-nav">
            <a href="#what">What</a>
            <a href="#why">Why</a>
            <a href="#how">How</a>
            <a href="#who">For who</a>
            <a href="#cta">Download</a>
          </nav>
        </div>
        <div class="footer-bottom">
          <p>&copy; 2025 Citizen's Eye. All rights reserved.</p>
          <div class="footer-legal">
            <a href="#">Privacy</a>
            <a href="#">Terms</a>
            <a href="#">Instagram</a>
            <a href="#">TikTok</a>
          </div>
        </div>
      </div>
    </footer>

  </body>
</html>

<script>
  // Burger menu
  const burger   = document.getElementById('burger') as HTMLButtonElement;
  const drawer   = document.getElementById('navDrawer') as HTMLDivElement;
  const mainNav  = document.getElementById('mainNav') as HTMLElement;

  burger?.addEventListener('click', () => {
    const open = drawer.classList.toggle('open');
    burger.classList.toggle('open', open);
    document.body.style.overflow = open ? 'hidden' : '';
  });

  drawer?.querySelectorAll('a').forEach(a => a.addEventListener('click', () => {
    drawer.classList.remove('open');
    burger.classList.remove('open');
    document.body.style.overflow = '';
  }));

  // Nav scroll style
  window.addEventListener('scroll', () => {
    mainNav?.classList.toggle('scrolled', window.scrollY > 48);
  }, { passive: true });

  // Scroll reveals
  const io = new IntersectionObserver((entries) => {
    entries.forEach(e => {
      if (e.isIntersecting) {
        const el = e.target as HTMLElement;
        const si = parseFloat(el.style.getPropertyValue('--si') || '0');
        el.style.transitionDelay = (si * 0.1) + 's';
        el.classList.add('revealed');
        io.unobserve(el);
      }
    });
  }, { threshold: 0.12 });

  document.querySelectorAll('.reveal').forEach(el => io.observe(el));

  // CTA form
  document.getElementById('mainForm')?.addEventListener('submit', e => {
    e.preventDefault();
    const form = e.target as HTMLFormElement;
    const btn  = form.querySelector('button') as HTMLButtonElement;
    const inp  = form.querySelector('input')  as HTMLInputElement;
    btn.textContent = "You're on the list ✓";
    btn.disabled = inp.disabled = true;
  });
</script>

<style>
  /* ── RESET & BASE ── */
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  :root {
    --ink:    #0a0f1a;
    --ink2:   #111827;
    --blue:   #2563eb;
    --blue2:  #1d4ed8;
    --white:  #ffffff;
    --soft:   #f5f7fa;
    --border: #e2e8f0;
    --muted:  #64748b;
    --font:   'Inter', system-ui, sans-serif;
  }

  html { scroll-behavior: smooth; }

  body {
    font-family: var(--font);
    background: var(--white);
    color: var(--ink);
    -webkit-font-smoothing: antialiased;
    overflow-x: hidden;
  }

  /* ── ANIMATIONS ── */
  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(32px); }
    to   { opacity: 1; transform: translateY(0); }
  }
  @keyframes ticker {
    from { transform: translateX(0); }
    to   { transform: translateX(-50%); }
  }
  @keyframes ping {
    0%, 100% { transform: scale(1); opacity: 1; }
    50%       { transform: scale(2); opacity: 0; }
  }

  .fade-up {
    opacity: 0;
    animation: fadeUp 0.9s cubic-bezier(0.16,1,0.3,1) var(--d, 0s) forwards;
  }

  .reveal {
    opacity: 0;
    transform: translateY(36px);
    transition: opacity 0.75s cubic-bezier(0.16,1,0.3,1),
                transform 0.75s cubic-bezier(0.16,1,0.3,1);
  }
  .revealed { opacity: 1; transform: translateY(0); }

  /* ── LAYOUT ── */
  .container {
    max-width: 1120px;
    margin: 0 auto;
    padding: 0 32px;
  }
  .section { padding: 112px 0; }
  .bg-white { background: var(--white); }
  .bg-ink   { background: var(--ink); }
  .bg-soft  { background: var(--soft); }

  /* ── TYPOGRAPHY ── */
  .eyebrow {
    display: inline-block;
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--blue);
    margin-bottom: 14px;
  }
  .eyebrow-light { color: rgba(147,197,253,0.8); }
  .white { color: var(--white) !important; }

  .section-h2 {
    font-size: clamp(2rem, 4.5vw, 3.4rem);
    font-weight: 800;
    letter-spacing: -0.035em;
    line-height: 1.08;
    color: var(--ink);
    margin-bottom: 24px;
  }

  .section-top { margin-bottom: 64px; }
  .br-desk { display: none; }
  .br-mob  { display: none; }

  /* ── BUTTONS ── */
  .btn-primary-sm {
    padding: 9px 20px;
    background: var(--blue);
    color: var(--white);
    border-radius: 8px;
    font-size: 0.85rem;
    font-weight: 600;
    text-decoration: none;
    transition: background 0.2s, transform 0.15s;
    white-space: nowrap;
  }
  .btn-primary-sm:hover { background: var(--blue2); transform: translateY(-1px); }

  .btn-outline-sm {
    display: inline-block;
    padding: 9px 20px;
    border: 1.5px solid var(--border);
    color: var(--ink);
    border-radius: 8px;
    font-size: 0.85rem;
    font-weight: 600;
    text-decoration: none;
    transition: border-color 0.2s, background 0.2s;
  }
  .btn-outline-sm:hover { border-color: var(--blue); background: #eff6ff; }

  /* ── NAV ── */
  .nav {
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 100;
    padding: 18px 32px;
    transition: background 0.3s, box-shadow 0.3s, padding 0.3s;
  }
  .nav.scrolled {
    background: rgba(255,255,255,0.92);
    backdrop-filter: blur(16px);
    box-shadow: 0 1px 0 var(--border);
    padding: 12px 32px;
  }

  .nav-inner {
    max-width: 1120px;
    margin: 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 24px;
  }

  .nav-logo {
    display: flex;
    align-items: center;
    gap: 9px;
    font-size: 0.95rem;
    font-weight: 700;
    color: var(--ink);
    text-decoration: none;
    letter-spacing: -0.01em;
    flex-shrink: 0;
  }

  .nav-links {
    display: flex;
    align-items: center;
    gap: 28px;
  }
  .nav-links a {
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--muted);
    text-decoration: none;
    transition: color 0.2s;
  }
  .nav-links a:hover { color: var(--ink); }

  .nav-actions { display: flex; align-items: center; gap: 10px; }

  /* Burger */
  .burger {
    display: none;
    flex-direction: column;
    justify-content: center;
    gap: 5px;
    width: 36px;
    height: 36px;
    background: transparent;
    border: 1.5px solid var(--border);
    border-radius: 8px;
    cursor: pointer;
    padding: 0 8px;
  }
  .burger span {
    display: block;
    height: 1.5px;
    background: var(--ink);
    border-radius: 2px;
    transition: transform 0.25s, opacity 0.2s;
    transform-origin: center;
  }
  .burger.open span:nth-child(1) { transform: translateY(6.5px) rotate(45deg); }
  .burger.open span:nth-child(2) { opacity: 0; transform: scaleX(0); }
  .burger.open span:nth-child(3) { transform: translateY(-6.5px) rotate(-45deg); }

  /* Drawer */
  .nav-drawer {
    display: none;
    position: fixed;
    inset: 0;
    top: 0;
    background: var(--white);
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 8px;
    z-index: 99;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.25s;
  }
  .nav-drawer.open {
    opacity: 1;
    pointer-events: all;
  }
  .nav-drawer a {
    font-size: 1.8rem;
    font-weight: 800;
    letter-spacing: -0.03em;
    color: var(--muted);
    text-decoration: none;
    padding: 8px 24px;
    transition: color 0.15s;
  }
  .nav-drawer a:hover { color: var(--ink); }
  .drawer-cta {
    margin-top: 20px;
    padding: 14px 40px !important;
    background: var(--blue) !important;
    color: var(--white) !important;
    border-radius: 12px;
    font-size: 1rem !important;
  }

  /* ── HERO ── */
  .hero {
    padding-top: 120px;
    background: var(--white);
    overflow: hidden;
    border-bottom: 1px solid var(--border);
  }

  .hero-inner {
    max-width: 1120px;
    margin: 0 auto;
    padding: 48px 32px 72px;
    display: grid;
    grid-template-columns: 1fr 1fr;
    align-items: center;
    gap: 64px;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    font-size: 0.72rem;
    font-weight: 600;
    letter-spacing: 0.05em;
    color: var(--blue);
    background: #eff6ff;
    border: 1px solid #bfdbfe;
    border-radius: 100px;
    padding: 5px 14px;
    margin-bottom: 24px;
  }

  .badge-ping {
    width: 7px; height: 7px;
    background: var(--blue);
    border-radius: 50%;
    animation: ping 2s ease-in-out infinite;
  }

  .hero-h1 {
    font-size: clamp(3.2rem, 7vw, 6rem);
    font-weight: 900;
    letter-spacing: -0.05em;
    line-height: 1;
    color: var(--ink);
  }
  .hero-h1 em {
    font-style: normal;
    color: var(--muted);
    font-weight: 300;
  }
  .h1-change {
    color: var(--blue);
    display: block;
  }

  .hero-desc {
    font-size: 1.05rem;
    color: var(--muted);
    line-height: 1.75;
    margin-bottom: 32px;
    max-width: 400px;
  }

  .hero-stores {
    display: flex;
    gap: 10px;
    flex-wrap: wrap;
  }

  .store-pill {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 10px 18px;
    background: var(--ink);
    color: var(--white);
    border-radius: 10px;
    font-size: 0.85rem;
    font-weight: 600;
    text-decoration: none;
    transition: background 0.2s, transform 0.15s;
  }
  .store-pill:hover { background: var(--ink2); transform: translateY(-1px); }

  /* Ticker */
  .hero-ticker {
    border-top: 1px solid var(--border);
    padding: 14px 0;
    overflow: hidden;
    white-space: nowrap;
  }
  .ticker-track {
    display: inline-flex;
    gap: 20px;
    animation: ticker 30s linear infinite;
  }
  .ticker-track span {
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--muted);
    flex-shrink: 0;
  }
  .ticker-dot { color: var(--blue) !important; }

  /* ── WHAT GRID ── */
  .what-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
  }

  .what-card {
    border: 1.5px solid var(--border);
    border-radius: 16px;
    padding: 28px 24px;
    background: var(--white);
    transition: border-color 0.2s, transform 0.2s, box-shadow 0.2s;
  }
  .what-card:hover {
    border-color: #bfdbfe;
    transform: translateY(-2px);
    box-shadow: 0 12px 32px rgba(37,99,235,0.07);
  }
  .what-card-cta {
    background: var(--soft);
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    gap: 20px;
  }

  .wc-icon {
    width: 44px; height: 44px;
    background: color-mix(in srgb, var(--cc) 10%, transparent);
    border-radius: 11px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--cc);
    margin-bottom: 16px;
  }
  .what-card h3 {
    font-size: 0.95rem;
    font-weight: 700;
    margin-bottom: 8px;
    color: var(--ink);
  }
  .what-card p, .wc-cta-text {
    font-size: 0.85rem;
    color: var(--muted);
    line-height: 1.65;
  }

  /* ── WHY ── */
  .why-layout {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 80px;
    align-items: start;
  }
  .why-body {
    font-size: 1rem;
    color: rgba(255,255,255,0.6);
    line-height: 1.8;
    margin-bottom: 16px;
  }
  .why-numbers {
    display: flex;
    gap: 40px;
    margin-top: 40px;
    padding-top: 32px;
    border-top: 1px solid rgba(255,255,255,0.1);
    flex-wrap: wrap;
  }
  .why-num { display: flex; flex-direction: column; gap: 4px; }
  .wn-val {
    font-size: 2.4rem;
    font-weight: 900;
    letter-spacing: -0.04em;
    color: var(--white);
    line-height: 1;
  }
  .wn-label {
    font-size: 0.7rem;
    color: rgba(255,255,255,0.4);
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }

  /* ── HOW IT WORKS ── */
  .steps {
    display: flex;
    align-items: flex-start;
    gap: 0;
    flex-wrap: wrap;
  }

  .step {
    flex: 1;
    min-width: 140px;
    padding: 24px 20px;
    background: var(--white);
    border: 1.5px solid var(--border);
    border-radius: 16px;
    transition: border-color 0.2s, box-shadow 0.2s;
  }
  .step:hover {
    border-color: #bfdbfe;
    box-shadow: 0 8px 24px rgba(37,99,235,0.07);
  }

  .step-connector {
    width: 24px;
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    padding-top: 36px;
  }
  .step-connector::after {
    content: '→';
    color: #cbd5e1;
    font-size: 1rem;
  }

  .step-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 16px;
  }
  .step-n {
    font-size: 0.62rem;
    font-weight: 800;
    letter-spacing: 0.1em;
    color: var(--blue);
    opacity: 0.6;
  }
  .step-icon {
    width: 36px; height: 36px;
    border-radius: 9px;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .si-blue   { background: var(--blue); }
  .si-amber  { background: #f59e0b; }
  .si-green  { background: #10b981; }
  .si-purple { background: #8b5cf6; }

  .step h3 {
    font-size: 0.85rem;
    font-weight: 700;
    color: var(--ink);
    margin-bottom: 8px;
    line-height: 1.3;
  }
  .step p { font-size: 0.78rem; color: var(--muted); line-height: 1.6; }

  /* ── FOR WHO ── */
  .who-layout {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 80px;
    align-items: center;
  }
  .who-body {
    font-size: 1rem;
    color: var(--muted);
    line-height: 1.8;
    margin-bottom: 28px;
  }
  .who-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
  }
  .who-tags span {
    padding: 6px 14px;
    background: var(--white);
    border: 1.5px solid var(--border);
    border-radius: 100px;
    font-size: 0.8rem;
    font-weight: 500;
    color: var(--muted);
    transition: border-color 0.2s, color 0.2s;
  }
  .who-tags span:hover { border-color: var(--blue); color: var(--blue); }

  .pullquote {
    font-size: clamp(1.8rem, 3.5vw, 2.6rem);
    font-weight: 900;
    letter-spacing: -0.04em;
    line-height: 1.15;
    color: var(--ink);
    border-left: 4px solid var(--blue);
    padding-left: 28px;
  }

  /* ── CTA ── */
  .cta-section { text-align: center; }

  .cta-inner {
    max-width: 560px;
    margin: 0 auto;
  }

  .cta-sub {
    font-size: 1rem;
    color: rgba(255,255,255,0.55);
    line-height: 1.7;
    margin-bottom: 36px;
  }

  .cta-form { margin-bottom: 16px; }

  .cta-input-wrap {
    display: flex;
    gap: 10px;
    margin-bottom: 10px;
  }

  .cta-input {
    flex: 1;
    padding: 14px 16px;
    background: rgba(255,255,255,0.07);
    border: 1px solid rgba(255,255,255,0.15);
    border-radius: 10px;
    color: var(--white);
    font-size: 0.9rem;
    font-family: var(--font);
    outline: none;
    min-width: 0;
    transition: border-color 0.2s;
  }
  .cta-input::placeholder { color: rgba(255,255,255,0.3); }
  .cta-input:focus { border-color: rgba(59,130,246,0.6); }

  .cta-submit {
    padding: 14px 24px;
    background: var(--blue);
    color: var(--white);
    border: none;
    border-radius: 10px;
    font-size: 0.9rem;
    font-weight: 700;
    cursor: pointer;
    font-family: var(--font);
    white-space: nowrap;
    transition: background 0.2s;
  }
  .cta-submit:hover { background: var(--blue2); }

  .cta-note {
    font-size: 0.72rem;
    color: rgba(255,255,255,0.25);
  }

  .cta-stores {
    display: flex;
    gap: 12px;
    justify-content: center;
    flex-wrap: wrap;
    margin-top: 32px;
    padding-top: 32px;
    border-top: 1px solid rgba(255,255,255,0.08);
  }

  .cta-store-btn {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 12px 20px;
    background: rgba(255,255,255,0.06);
    border: 1px solid rgba(255,255,255,0.12);
    border-radius: 12px;
    color: var(--white);
    text-decoration: none;
    transition: background 0.2s, border-color 0.2s;
  }
  .cta-store-btn:hover { background: rgba(255,255,255,0.11); border-color: rgba(255,255,255,0.25); }
  .cta-store-btn div { text-align: left; line-height: 1.25; }
  .cta-store-btn span { display: block; font-size: 0.58rem; opacity: 0.5; }
  .cta-store-btn strong { display: block; font-size: 0.88rem; font-weight: 700; }

  /* ── FOOTER ── */
  .footer {
    background: var(--soft);
    border-top: 1px solid var(--border);
    padding: 40px 0 28px;
  }

  .footer-top {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 20px;
    margin-bottom: 24px;
    padding-bottom: 24px;
    border-bottom: 1px solid var(--border);
  }

  .footer-logo { font-size: 0.9rem; }

  .footer-nav {
    display: flex;
    gap: 24px;
    flex-wrap: wrap;
  }
  .footer-nav a {
    font-size: 0.85rem;
    color: var(--muted);
    text-decoration: none;
    transition: color 0.2s;
  }
  .footer-nav a:hover { color: var(--ink); }

  .footer-bottom {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 12px;
  }
  .footer-bottom p { font-size: 0.75rem; color: #94a3b8; }

  .footer-legal {
    display: flex;
    gap: 20px;
    flex-wrap: wrap;
  }
  .footer-legal a {
    font-size: 0.75rem;
    color: #94a3b8;
    text-decoration: none;
    transition: color 0.2s;
  }
  .footer-legal a:hover { color: var(--ink); }

  /* ── RESPONSIVE ── */

  /* Desktop line breaks */
  @media (min-width: 900px) {
    .br-desk { display: block; }
  }

  /* Tablet */
  @media (max-width: 900px) {
    .nav { padding: 14px 20px; }
    .nav.scrolled { padding: 10px 20px; }
    .nav-links { display: none; }
    .burger { display: flex; }
    .nav-drawer { display: flex; }

    .container { padding: 0 20px; }
    .section { padding: 80px 0; }

    .hero-inner {
      grid-template-columns: 1fr;
      gap: 40px;
      padding: 40px 20px 60px;
      text-align: center;
    }
    .hero-desc { margin: 0 auto 32px; }
    .hero-stores { justify-content: center; }

    .what-grid { grid-template-columns: repeat(2, 1fr); }
    .section-top { margin-bottom: 48px; }

    .why-layout { grid-template-columns: 1fr; gap: 48px; }
    .why-numbers { gap: 28px; }

    .steps { gap: 10px; }
    .step-connector { display: none; }
    .step { flex: calc(50% - 5px); min-width: calc(50% - 5px); }

    .who-layout { grid-template-columns: 1fr; gap: 40px; }
    .pullquote { border-left: none; padding-left: 0; border-top: 4px solid var(--blue); padding-top: 24px; }

    .cta-input-wrap { flex-direction: column; }
    .cta-submit { width: 100%; }

    .footer-top { flex-direction: column; align-items: flex-start; }
    .footer-bottom { flex-direction: column; align-items: flex-start; }
    .footer-nav { gap: 16px; }
  }

  /* Mobile */
  @media (max-width: 600px) {
    .nav { padding: 12px 16px; }
    .btn-primary-sm { display: none; }

    .container { padding: 0 16px; }
    .section { padding: 64px 0; }
    .section-h2 { font-size: clamp(1.75rem, 7.5vw, 2.4rem); }
    .section-top { margin-bottom: 36px; }

    .hero-inner { padding: 32px 16px 52px; gap: 32px; }
    .hero-h1 { font-size: clamp(2.8rem, 13vw, 4.2rem); }
    .hero-badge { font-size: 0.66rem; }
    .hero-desc { font-size: 0.95rem; }
    .store-pill { width: 100%; justify-content: center; }
    .hero-stores { flex-direction: column; gap: 10px; max-width: 280px; margin: 0 auto; }

    .what-grid { grid-template-columns: 1fr; gap: 12px; }
    .what-card { padding: 22px 18px; }

    .why-layout { gap: 36px; }
    .why-numbers { gap: 20px; }
    .wn-val { font-size: 1.9rem; }

    .steps { flex-direction: column; gap: 10px; }
    .step { flex: 1; min-width: 100%; }
    .br-mob { display: block; }

    .who-layout { gap: 32px; }
    .pullquote { font-size: clamp(1.5rem, 7vw, 2rem); }

    .cta-stores { flex-direction: column; align-items: center; }
    .cta-store-btn { width: 100%; max-width: 280px; justify-content: center; }

    .footer-legal { gap: 14px; }
    .footer-nav { flex-direction: column; gap: 10px; }
  }

  /* Small phones */
  @media (max-width: 380px) {
    .hero-h1 { font-size: clamp(2.4rem, 12vw, 3.4rem); }
    .pullquote { font-size: 1.4rem; }
    .what-card { padding: 18px 16px; }
  }
</style>
